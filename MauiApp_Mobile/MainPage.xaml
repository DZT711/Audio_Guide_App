                                            <?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp_Mobile.MainPage"
             Title="Smart Tourism">

    <!-- <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18">
            <VerticalStackLayout Spacing="6">
                <Label Text="Smart Tourism Location Tracker"
                       Style="{StaticResource Headline}"
                       SemanticProperties.HeadingLevel="Level1" />

                <Label Text="The app follows your device position when permission is granted. If location is unavailable, the default pin stays in District 4, Ho Chi Minh City."
                       Style="{StaticResource SubHeadline}" />
            </VerticalStackLayout>

            <Border x:Name="StatusCard"
                    BackgroundColor="#EEF6FF"
                    Stroke="#C7DCF7"
                    StrokeShape="RoundRectangle 24"
                    Padding="20">
                <VerticalStackLayout Spacing="14">
                    <Label x:Name="PositionStatusLabel"
                           FontAttributes="Bold"
                           FontSize="22"
                           Text="Checking your location..." />

                    <Label x:Name="PositionDetailLabel"
                           FontSize="14"
                           TextColor="#5A6372"
                           Text="The first refresh asks for location permission and then keeps the position updated automatically." />

                    <Grid ColumnDefinitions="*,*"
                          RowDefinitions="Auto,Auto,Auto"
                          ColumnSpacing="18"
                          RowSpacing="12">
                        <VerticalStackLayout Grid.Row="0" Grid.Column="0" Spacing="4">
                            <Label Text="Source" FontAttributes="Bold" />
                            <Label x:Name="PositionSourceValue" Text="Pending" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="4">
                            <Label Text="Area" FontAttributes="Bold" />
                            <Label x:Name="PositionAreaValue" Text="District 4, Ho Chi Minh City" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="1" Grid.Column="0" Spacing="4">
                            <Label Text="Latitude" FontAttributes="Bold" />
                            <Label x:Name="LatitudeValue" Text="10.754027" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="1" Grid.Column="1" Spacing="4">
                            <Label Text="Longitude" FontAttributes="Bold" />
                            <Label x:Name="LongitudeValue" Text="106.705412" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Spacing="4">
                            <Label Text="Last Updated" FontAttributes="Bold" />
                            <Label x:Name="LastUpdatedValue" Text="Not refreshed yet" />
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Button x:Name="RefreshLocationButton"
                    Text="Refresh Device Location"
                    Clicked="OnRefreshLocationClicked"
                    HorizontalOptions="Fill" />

            <ActivityIndicator x:Name="TrackingIndicator"
                               IsVisible="False"
                               IsRunning="False"
                               Color="#1E5AA8"
                               HorizontalOptions="Center" />

            <Border BackgroundColor="#F8F5ED"
                    Stroke="#E2D9C7"
                    StrokeShape="RoundRectangle 20"
                    Padding="18">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Shared default position"
                           FontAttributes="Bold"
                           FontSize="18" />

                    <Label Text="The admin web map and the mobile app now use the same fallback coordinates from the shared library, so both platforms start from the same District 4 default."
                           FontSize="14"
                           TextColor="#5A6372" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView> -->

</ContentPage>
